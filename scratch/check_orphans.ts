
import { DataSource } from 'typeorm';
import { Espacio } from './src/espacios/espacio.entity';
import { Embarcacion } from './src/embarcaciones/embarcaciones.entity';

async function checkOrphans() {
  // This is a pseudo-code for a manual check if we had a REPL
  // But I can't run it directly without bootstrapping Nest.
}
